@model Pets_friends.Data.ViewModels.LoginVM

@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_AuthLayout.cshtml"; // Force the full-screen layout
}

<div class="main-wrapper">
    <div class="main-container">

        <div class="left-panel">
            <img src="~/images/Pets.png" alt="Happy Pets" asp-append-version="true" />
        </div>

        <div class="right-panel">
            <div class="form-box">

                <div class="logo">
                    <span class="logo-text">Pet Friends</span>
                </div>

                <div class="toggle">
                    <!-- Added asp-route-returnUrl here so they don't lose their place if they switch to Register -->
                    <a asp-controller="Account" asp-action="Register" asp-route-returnUrl="@ViewData["ReturnUrl"]">Sign Up</a>
                    <a class="active">Log In</a>
                </div>

                <h2>Welcome Back</h2>

                <!-- Added asp-route-returnUrl here to pass the URL to the POST method -->
                <form asp-action="Login" method="post" asp-route-returnUrl="@ViewData["ReturnUrl"]">
                    <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

                    <div class="input-group">
                        <input asp-for="EmailAddress" placeholder="Email" />
                        <span asp-validation-for="EmailAddress" class="field-validation-error"></span>
                    </div>

                    <div class="input-group">
                        <input asp-for="Password" type="password" placeholder="Password" />
                        <span asp-validation-for="Password" class="field-validation-error"></span>
                    </div>

                    <div class="forgot-link">
                        <a asp-action="ForgotPassword">Forgot Password?</a>
                    </div>

                    <button type="submit">Login</button>
                </form>

            </div>
        </div>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}